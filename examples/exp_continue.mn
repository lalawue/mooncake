
math.randomseed(os.time())
value = math.random(1, 20)

for i=1, 20 {
    if i < value {
        continue
    }
    for j=20, 1, -1 {
        if j > value {
            continue
        }
        do {
            print(i + j + value)
            return
        }
    }
}
